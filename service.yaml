---
apiVersion: v1
kind: Service
metadata:
  name: node-metrics
  labels:
    app: node-metrics
spec:
  selector:
    app: node-metrics
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      protocol: TCP
